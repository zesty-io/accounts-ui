import { request } from '../../../../util/request'
import config from '../../../../shell/config'

export function teams(state = {}, action) {
  switch (action.type) {
    default:
      return state
  }
}

// CRUD teams here